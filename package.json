{
	"name": "template-v-ad",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev --host",
		"clean": "rm -rf .astro/cache .vite dist && pnpm store prune",
		"build": "pnpm run clean && astro check && astro build --prerender && astro build",
		"preview": "astro build && wrangler dev",
		"astro": "astro",
		"deploy": "astro build && wrangler deploy && pnpm run github",
		"cf-typegen": "wrangler types",
		"github": "git add . && git commit -m \"Deploy: $(date +'%Y-%m-%d %H:%M:%S')\" && git push origin HEAD",
		"optimize:preloads": "node scripts-optimizations/PrecargaInteligente.js",
		"optimize:full": "pnpm run build && pnpm run optimize:preloads"
	},
	"dependencies": {
		"@astrojs/check": "^0.9.4",
		"@astrojs/cloudflare": "^12.6.3",
		"astro": "^5.12.9",
		"tailwindcss": "^4.1.11",
		"typescript": "^5.9.2"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.1.11",
		"@types/node": "^24.2.1",
		"glob": "^11.0.3",
		"vite-plugin-compression": "^0.5.1",
		"wrangler": "^4.28.1"
	}
}